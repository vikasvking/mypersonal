.row
  .col-md-9
    .panel.panel-primary
      .panel-heading
        %h3
          Welcome Vikas
          Today is
          = Time.now.strftime('%A')
      .panel-body
        %p
          %h3
            Last thing you read was
            - if  Task.order('updated_at DESC').first!=nil
              = Task.order('updated_at DESC').first.name.split(".").join(" ")
        %p
          %h3
            Toady You have to read
            - if Time.now.strftime("%A")=='Monday'
              History
            - elsif   Time.now.strftime("%A")=='Tuesday'
              Geography
            - elsif Time.now.strftime("%A")=='Wednesday'
              Political Science
            - elsif Time.now.strftime("%A")=='Thrusday'
              Economics
            - elsif Time.now.strftime("%A")=='Friday'
              Mechanical
            - elsif Time.now.strftime("%A")=='Saturday'
              Current Affairs
            - else
              Revision
        %p
          %h3
            you have
            = Pending.count
            pending work
        %p
          %h3
            - Todo.all.each do |i|
              %b= i.name
              task has
              %b= i.tasks.where(done: true).count
              task done out of
              %b= i.tasks.count

      .panel-footer
        = link_to "New todo",new_todo_path,class: 'btn btn-info'
        = link_to "New Book",new_study_path,class: 'btn btn-info'

  .col-md-3