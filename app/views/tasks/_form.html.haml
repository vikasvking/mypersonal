= form_for ([@todo,@todo.tasks.build]) do |f|


  .form-group
    = f.label :name
    = f.select(:name,Chapter.all.collect{|d| d.name+"."+Study.find_by_id(d.study_id).book+"."+Study.find_by_id(d.study_id).subject},{include_blank: true})
  .actions
    = f.submit 'Save',class: 'btn btn-primary'
